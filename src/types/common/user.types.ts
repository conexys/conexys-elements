/**
 * @fileoverview
 */

export interface RoleItem {
  id: string | number;
  user_type: string;
}

export interface BaseUserData {
  name: string;
  lastname: string;
  email: string;
  username: string;
  language: string;
}
